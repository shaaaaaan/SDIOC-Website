# 1. Prevent the core layout entry file from evaluation fallback loops
/index.html    /index.html    200

# 2. Let the edge router fallback cleanly for single-page client routing
/* /index.html    200
